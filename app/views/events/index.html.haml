%h1 Listing events

%table
  %tr
    %th Venue
    %th Title
    %th Description
    %th

  - @events.each do |event|
    %tr
      %td= event.venue_id
      %td= event.title
      %td= event.description
      %td= link_to 'Show', event
%br
